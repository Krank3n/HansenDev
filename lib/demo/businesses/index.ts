import { DemoBusiness } from '../types';
import { instantalloy } from './instantalloy';
import { brbb } from './brbb';
import { beachesfp } from './beachesfp';

export const demoBusinesses: Record<string, DemoBusiness> = {
  instantalloy,
  brbb,
  beachesfp,
};
